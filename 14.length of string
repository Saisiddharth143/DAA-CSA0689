#include <stdio.h>
int main()
{
    char s[100]; 
    int l=0;
    printf("Enter a string: ");
    gets(s); 
    while (s[l] != '\0') {
        l++;
    }
    printf("Length of the string: %d\n", l);
    return 0;
}

